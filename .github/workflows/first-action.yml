name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: | 
          echo "Hello World"
          echo "My name is Sunny"
      - name: Print goodbye
        run: echo "Done. Byee"
